# Manuscripts under review, rendered as the academic CV's "Under Review"
# section. Same shape as preprints.yaml: structured `authors` plus an optional
# `venue` annotation.
#
# Deliberately empty. The academic CV suppresses the whole section (header and
# rule included) while this list has no entries, so re-adding an item here is
# all it takes to bring the section back.
[]
